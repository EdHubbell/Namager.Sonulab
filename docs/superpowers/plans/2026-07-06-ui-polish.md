# Studio-Warm UI Polish + Amps Master-Detail Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give the app a cohesive "Studio warm" skin (amber accent, warm dark/light palettes) via one shared theme layer, and restructure the Amps tab into a master-detail layout with a right-hand panel hosting details and the upload form.

**Architecture:** One new `Styles/SonulabTheme.axaml` (ThemeVariant-scoped palette tokens + style classes) merged into `App.axaml`, plus Fluent accent-ramp overrides in `Application.Resources`. `AmpListView` becomes a `260,*` grid whose right column is a new `AmpDetailPanel` UserControl (inherited DataContext, three exclusive states). All other views get styling-attribute changes only.

**Tech Stack:** Avalonia 12 with built-in FluentTheme, compiled bindings, xUnit (existing suite — untouched).

**Spec:** `docs/superpowers/specs/2026-07-06-ui-polish-design.md` — read it first. The visual target is the brainstorm mockup `.superpowers/brainstorm/14837-1783373193/content/final-look.html` (gitignored; open in a browser if present).

## Global Constraints

- **Zero ViewModel/service changes. Zero test edits. `dotnet test` must report 312/312 passing after every task.**
- Avalonia 12 + built-in `FluentTheme` only. Do NOT add FluentAvalonia or any package. No `ControlTemplate` replacements — style setters and resources only (`/template/` part-targeting selectors are allowed; full template swaps are not).
- Every palette token must be defined in BOTH `Dark` and `Light` theme dictionaries. A token present in one variant only is a bug.
- Every XAML binding referenced must already exist on the ViewModels — if a binding you're adding doesn't exist, you've deviated; stop and re-read the spec.
- Palette (exact values, from the spec):
  | Token | Dark | Light |
  |---|---|---|
  | `Sonulab.AccentColor` | `#D9820F` | `#B96A08` |
  | `Sonulab.AccentHoverColor` | `#E8A04A` | `#D9820F` |
  | `Sonulab.SurfaceBrush` | `#26221E` | `#FFFDF9` |
  | `Sonulab.SurfaceAltBrush` | `#1E1B18` | `#F6F2EC` |
  | `Sonulab.BorderBrush` | `#3A332C` | `#DDD3C4` |
  | `Sonulab.RuleBrush` | `#332D27` | `#EDE5D8` |
  | `Sonulab.TextMutedBrush` | `#E8E2DA` @55% | `#2B2620` @55% |
  | `Sonulab.SuccessBrush` | `#7BC47B` | `#4A9E4A` |
  | `Sonulab.DangerBrush` | `#E06C68` | `#C0392B` |
  | `Sonulab.WarningBrush` | `#E8A04A` | `#B96A08` |
- Run all commands from the repo root `C:\Development\Buckdrivers\Sonulab\StompStationManager`.
- COMMIT HYGIENE: stage ONLY the files each task names, by exact path. NEVER `git add -A`, `git add .`, or `git commit -a` — the working tree contains unrelated untracked files.

---

### Task 1: Theme layer (`SonulabTheme.axaml` + App.axaml wiring)

**Files:**
- Create: `src/Sonulab.App/Styles/SonulabTheme.axaml`
- Modify: `src/Sonulab.App/App.axaml`

**Interfaces:**
- Consumes: nothing new.
- Produces (later tasks rely on these EXACT names): dynamic-resource tokens `Sonulab.AccentBrush`, `Sonulab.AccentHoverBrush`, `Sonulab.AccentTintBrush`, `Sonulab.HoverTintBrush`, `Sonulab.SurfaceBrush`, `Sonulab.SurfaceAltBrush`, `Sonulab.BorderBrush`, `Sonulab.RuleBrush`, `Sonulab.TextMutedBrush`, `Sonulab.SuccessBrush`, `Sonulab.DangerBrush`, `Sonulab.WarningBrush`; style classes `card`, `rule`, `section-label`, `slot-number`, `accent-outline`; global `Window`/`SplitView`/`ListBoxItem` styles.

- [ ] **Step 1: Create `src/Sonulab.App/Styles/SonulabTheme.axaml`**

```xml
<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ===== Studio-warm palette tokens (spec §1.1). Both variants ALWAYS defined. ===== -->
  <Styles.Resources>
    <ResourceDictionary>
      <ResourceDictionary.ThemeDictionaries>
        <ResourceDictionary x:Key="Dark">
          <Color x:Key="Sonulab.AccentColor">#D9820F</Color>
          <Color x:Key="Sonulab.AccentHoverColor">#E8A04A</Color>
          <SolidColorBrush x:Key="Sonulab.AccentBrush" Color="#D9820F"/>
          <SolidColorBrush x:Key="Sonulab.AccentHoverBrush" Color="#E8A04A"/>
          <SolidColorBrush x:Key="Sonulab.AccentTintBrush" Color="#D9820F" Opacity="0.16"/>
          <SolidColorBrush x:Key="Sonulab.HoverTintBrush" Color="#D9820F" Opacity="0.08"/>
          <SolidColorBrush x:Key="Sonulab.SurfaceBrush" Color="#26221E"/>
          <SolidColorBrush x:Key="Sonulab.SurfaceAltBrush" Color="#1E1B18"/>
          <SolidColorBrush x:Key="Sonulab.BorderBrush" Color="#3A332C"/>
          <SolidColorBrush x:Key="Sonulab.RuleBrush" Color="#332D27"/>
          <SolidColorBrush x:Key="Sonulab.TextMutedBrush" Color="#E8E2DA" Opacity="0.55"/>
          <SolidColorBrush x:Key="Sonulab.SuccessBrush" Color="#7BC47B"/>
          <SolidColorBrush x:Key="Sonulab.DangerBrush" Color="#E06C68"/>
          <SolidColorBrush x:Key="Sonulab.WarningBrush" Color="#E8A04A"/>
        </ResourceDictionary>
        <ResourceDictionary x:Key="Light">
          <Color x:Key="Sonulab.AccentColor">#B96A08</Color>
          <Color x:Key="Sonulab.AccentHoverColor">#D9820F</Color>
          <SolidColorBrush x:Key="Sonulab.AccentBrush" Color="#B96A08"/>
          <SolidColorBrush x:Key="Sonulab.AccentHoverBrush" Color="#D9820F"/>
          <SolidColorBrush x:Key="Sonulab.AccentTintBrush" Color="#B96A08" Opacity="0.14"/>
          <SolidColorBrush x:Key="Sonulab.HoverTintBrush" Color="#B96A08" Opacity="0.07"/>
          <SolidColorBrush x:Key="Sonulab.SurfaceBrush" Color="#FFFDF9"/>
          <SolidColorBrush x:Key="Sonulab.SurfaceAltBrush" Color="#F6F2EC"/>
          <SolidColorBrush x:Key="Sonulab.BorderBrush" Color="#DDD3C4"/>
          <SolidColorBrush x:Key="Sonulab.RuleBrush" Color="#EDE5D8"/>
          <SolidColorBrush x:Key="Sonulab.TextMutedBrush" Color="#2B2620" Opacity="0.55"/>
          <SolidColorBrush x:Key="Sonulab.SuccessBrush" Color="#4A9E4A"/>
          <SolidColorBrush x:Key="Sonulab.DangerBrush" Color="#C0392B"/>
          <SolidColorBrush x:Key="Sonulab.WarningBrush" Color="#B96A08"/>
        </ResourceDictionary>
      </ResourceDictionary.ThemeDictionaries>
    </ResourceDictionary>
  </Styles.Resources>

  <!-- ===== Chrome: warm window / nav backgrounds ===== -->
  <Style Selector="Window">
    <Setter Property="Background" Value="{DynamicResource Sonulab.SurfaceAltBrush}"/>
  </Style>
  <Style Selector="SplitView">
    <Setter Property="PaneBackground" Value="{DynamicResource Sonulab.SurfaceAltBrush}"/>
  </Style>

  <!-- ===== Cards and rules (spec §1.3) ===== -->
  <Style Selector="Border.card">
    <Setter Property="Background" Value="{DynamicResource Sonulab.SurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Sonulab.BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="6"/>
    <Setter Property="Padding" Value="12"/>
  </Style>
  <Style Selector="Border.rule">
    <Setter Property="BorderBrush" Value="{DynamicResource Sonulab.RuleBrush}"/>
    <Setter Property="BorderThickness" Value="0,1,0,0"/>
    <Setter Property="Margin" Value="0,8,0,6"/>
    <Setter Property="Height" Value="1"/>
  </Style>

  <!-- ===== Typography ===== -->
  <Style Selector="TextBlock.section-label">
    <Setter Property="FontSize" Value="10"/>
    <Setter Property="LetterSpacing" Value="1"/>
    <Setter Property="Foreground" Value="{DynamicResource Sonulab.TextMutedBrush}"/>
  </Style>
  <Style Selector="TextBlock.slot-number">
    <Setter Property="FontFamily" Value="Consolas,Cascadia Mono,monospace"/>
    <Setter Property="Foreground" Value="{DynamicResource Sonulab.TextMutedBrush}"/>
  </Style>

  <!-- ===== Accent-outlined button ===== -->
  <Style Selector="Button.accent-outline">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Sonulab.AccentBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Foreground" Value="{DynamicResource Sonulab.AccentBrush}"/>
  </Style>
  <Style Selector="Button.accent-outline:pointerover /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource Sonulab.HoverTintBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Sonulab.AccentHoverBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource Sonulab.AccentHoverBrush}"/>
  </Style>

  <!-- ===== List rows: amber left bar + tint (spec §1.3). Fluent's ListBoxItem template
       binds Background/BorderBrush/BorderThickness onto PART_ContentPresenter, but its
       pseudo-class styles override Background there — so the bar rides the item-level
       Border* properties while tints target the presenter directly. ===== -->
  <Style Selector="ListBoxItem">
    <Setter Property="BorderThickness" Value="3,0,0,0"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="CornerRadius" Value="3"/>
    <Setter Property="Padding" Value="9,6"/>
    <Setter Property="MinHeight" Value="0"/>
  </Style>
  <Style Selector="ListBoxItem:selected">
    <Setter Property="BorderBrush" Value="{DynamicResource Sonulab.AccentBrush}"/>
  </Style>
  <Style Selector="ListBoxItem:selected /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource Sonulab.AccentTintBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Sonulab.AccentBrush}"/>
  </Style>
  <Style Selector="ListBoxItem:pointerover:not(:selected) /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource Sonulab.HoverTintBrush}"/>
  </Style>
</Styles>
```

- [ ] **Step 2: Wire it into `src/Sonulab.App/App.axaml`** — replace the whole file with:

```xml
<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Sonulab.App.App"
             xmlns:local="using:Sonulab.App"
             RequestedThemeVariant="Default">

    <Application.DataTemplates>
        <local:ViewLocator/>
    </Application.DataTemplates>

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceInclude Source="avares://Sonulab.App/Icons.axaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- Fluent accent-ramp override (spec §1.2): stock controls (selection,
                 focus, progress, toggles) adopt the Studio-warm amber. Lives in
                 Application.Resources so it's resolvable before any control loads. -->
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Dark">
                    <Color x:Key="SystemAccentColor">#D9820F</Color>
                    <Color x:Key="SystemAccentColorDark1">#B96A08</Color>
                    <Color x:Key="SystemAccentColorDark2">#9A5606</Color>
                    <Color x:Key="SystemAccentColorDark3">#7C4405</Color>
                    <Color x:Key="SystemAccentColorLight1">#E8A04A</Color>
                    <Color x:Key="SystemAccentColorLight2">#F0B76E</Color>
                    <Color x:Key="SystemAccentColorLight3">#F7CE96</Color>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <Color x:Key="SystemAccentColor">#B96A08</Color>
                    <Color x:Key="SystemAccentColorDark1">#9A5606</Color>
                    <Color x:Key="SystemAccentColorDark2">#7C4405</Color>
                    <Color x:Key="SystemAccentColorDark3">#5E3304</Color>
                    <Color x:Key="SystemAccentColorLight1">#D9820F</Color>
                    <Color x:Key="SystemAccentColorLight2">#E8A04A</Color>
                    <Color x:Key="SystemAccentColorLight3">#F0B76E</Color>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Application.Resources>

    <Application.Styles>
        <FluentTheme />
        <StyleInclude Source="avares://Sonulab.App/Styles/SonulabTheme.axaml"/>
    </Application.Styles>
</Application>
```

- [ ] **Step 3: Build + full suite**

Run: `dotnet build` — expected: succeeds, no new XAML (AVLN) errors.
Run: `dotnet test` — expected: 312/312 pass, zero test edits.

- [ ] **Step 4: Smoke-run and eyeball the accent**

Run: `dotnet run --project src/Sonulab.App` (VoidX-Control closed; device NOT required — the window opens disconnected). Verify: window background is warm (`#1E1B18` dark / `#F6F2EC` light per OS theme); nav selection shows the amber left bar + tint; focus rings/selection read amber, not blue. **Fallback if stock controls stay blue** (Fluent resolved the accent before the override): move the same `ThemeDictionaries` block INTO `SonulabTheme.axaml`'s `Styles.Resources` dictionary alongside the Sonulab tokens and retest; if still blue, additionally override `SystemControlHighlightListAccentLowBrush`/`SystemControlHighlightListAccentMediumLowBrush` per variant with `SolidColorBrush` amber tints. Close the app after checking.

- [ ] **Step 5: Commit**

```bash
git add src/Sonulab.App/Styles/SonulabTheme.axaml src/Sonulab.App/App.axaml
git commit -m "feat: Studio-warm theme layer (palette tokens, accent ramp, style classes)"
```

---

### Task 2: Amps master-detail (`AmpDetailPanel` + `AmpListView` grid)

**Files:**
- Create: `src/Sonulab.App/Views/AmpDetailPanel.axaml`
- Create: `src/Sonulab.App/Views/AmpDetailPanel.axaml.cs`
- Modify: `src/Sonulab.App/Views/AmpListView.axaml` (full rewrite below)
- NOT modified: `src/Sonulab.App/Views/AmpListView.axaml.cs` (its `UploadNamButton`/`UploadVxampButton`/`OnEditBoxLostFocus` references must keep resolving — the rewrite preserves those names)

**Interfaces:**
- Consumes: Task 1 tokens/classes; every binding already on `AmpListViewModel` (`IsUploadPanelOpen`, `IsDetailsVisible`, `IsDetailsLoading`, `ShowNoMetadata`, `DetailsError`, `DetailsFields`, `DetailsNotes`, `DetailsUrl`, `Selected.Name`, `Selected.DisplaySlot`, upload props, edit props — all exist today in `AmpListView.axaml`).
- Produces: `AmpDetailPanel` UserControl (inherited DataContext; no code-behind logic).

- [ ] **Step 1: Create `src/Sonulab.App/Views/AmpDetailPanel.axaml.cs`**

```csharp
using Avalonia.Controls;

namespace Sonulab.App.Views;

/// <summary>Right-hand panel of the Amps tab. Pure view: inherits AmpListViewModel as
/// DataContext and cycles upload form / details / placeholder off existing bindings.</summary>
public partial class AmpDetailPanel : UserControl
{
    public AmpDetailPanel() => InitializeComponent();
}
```

- [ ] **Step 2: Create `src/Sonulab.App/Views/AmpDetailPanel.axaml`**

State priority (spec §2): upload > details > placeholder. Details/placeholder are gated on `!IsUploadPanelOpen` via `BoolConverters.And` multi-bindings.

```xml
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Sonulab.App.ViewModels"
             xmlns:conv="using:Sonulab.App.Converters"
             x:Class="Sonulab.App.Views.AmpDetailPanel"
             x:DataType="vm:AmpListViewModel">
  <DockPanel>

    <!-- Messages always on top, whatever state is active (spec §2) -->
    <TextBlock DockPanel.Dock="Top" Margin="0,0,0,4" FontSize="11" TextWrapping="Wrap"
               Foreground="{DynamicResource Sonulab.WarningBrush}"
               Text="{Binding UploadBlockedMessage}"
               IsVisible="{Binding UploadBlockedMessage, Converter={x:Static ObjectConverters.IsNotNull}}"/>
    <TextBlock DockPanel.Dock="Top" Margin="0,0,0,4" FontSize="11" TextWrapping="Wrap"
               Foreground="{DynamicResource Sonulab.DangerBrush}"
               Text="{Binding ErrorMessage}"
               IsVisible="{Binding ErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}"/>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
      <Panel>

        <!-- ===== State 1: upload form ===== -->
        <Border Classes="card" VerticalAlignment="Top" IsVisible="{Binding IsUploadPanelOpen}">
          <StackPanel Spacing="8">
            <TextBlock Classes="section-label" Text="UPLOAD"/>
            <TextBlock Text="{Binding UploadSourceFileName}" FontWeight="SemiBold" FontSize="15"/>
            <Border Classes="rule"/>
            <Grid ColumnDefinitions="Auto,*,Auto,Auto" RowDefinitions="Auto">
              <TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,8,0"/>
              <TextBox Grid.Column="1" Text="{Binding UploadName}" MaxLength="31"
                       IsEnabled="{Binding !IsUploading}"/>
              <TextBlock Grid.Column="2" Text="Slot" VerticalAlignment="Center" Margin="12,0,8,0"/>
              <ComboBox Grid.Column="3" MinWidth="90"
                        ItemsSource="{Binding EmptySlots}"
                        SelectedItem="{Binding SelectedEmptySlot}"
                        IsEnabled="{Binding !IsUploading}">
                <ComboBox.ItemTemplate>
                  <DataTemplate x:DataType="x:Int32">
                    <TextBlock Text="{Binding Converter={x:Static conv:IndexToSlotNumber.Instance}}"/>
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </Grid>
            <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
              <TextBlock Grid.Row="0" Grid.Column="0" Text="Link" VerticalAlignment="Center" Margin="0,0,8,0"/>
              <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding UploadUrl}" Watermark="https:// where the tone came from (optional)"
                       IsEnabled="{Binding !IsUploading}" Margin="0,0,0,4"/>
              <TextBlock Grid.Row="1" Grid.Column="0" Text="Notes" VerticalAlignment="Top" Margin="0,4,8,0"/>
              <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding UploadNotes}" Watermark="Notes about this tone (optional)"
                       AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="96"
                       IsEnabled="{Binding !IsUploading}"/>
            </Grid>
            <TextBlock Text="{Binding NotesBudgetWarning}" FontSize="11" TextWrapping="Wrap"
                       Foreground="{DynamicResource Sonulab.WarningBrush}"
                       IsVisible="{Binding NotesBudgetWarning, Converter={x:Static ObjectConverters.IsNotNull}}"/>
            <ProgressBar Height="6" Minimum="0" Maximum="1"
                         Value="{Binding UploadProgressValue}"
                         IsIndeterminate="{Binding IsUploadIndeterminate}"
                         IsVisible="{Binding IsUploading}"/>
            <TextBlock Text="{Binding UploadStatus}" FontSize="11" Foreground="{DynamicResource Sonulab.TextMutedBrush}"/>
            <TextBlock Text="{Binding UploadError}" FontSize="11" TextWrapping="Wrap"
                       Foreground="{DynamicResource Sonulab.DangerBrush}"
                       IsVisible="{Binding UploadError, Converter={x:Static ObjectConverters.IsNotNull}}"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <Button Classes="accent-outline" Content="Start" Command="{Binding StartUploadCommand}" IsEnabled="{Binding !IsUploading}"/>
              <Button Content="Cancel" Command="{Binding CancelUploadCommand}" IsEnabled="{Binding CanCancelUpload}"
                      ToolTip.Tip="Cancelling is only possible while distilling — device writes always run to completion"/>
              <Button Content="Close" Command="{Binding CloseUploadPanelCommand}" IsEnabled="{Binding !IsUploading}"/>
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- ===== State 2: details ===== -->
        <Border Classes="card" VerticalAlignment="Top">
          <Border.IsVisible>
            <MultiBinding Converter="{x:Static BoolConverters.And}">
              <Binding Path="IsDetailsVisible"/>
              <Binding Path="!IsUploadPanelOpen"/>
            </MultiBinding>
          </Border.IsVisible>
          <StackPanel Spacing="4">
            <StackPanel Orientation="Horizontal" Spacing="10">
              <TextBlock Text="{Binding Selected.Name}" FontWeight="SemiBold" FontSize="15"/>
              <TextBlock Text="{Binding Selected.DisplaySlot, StringFormat='SLOT {0:00}'}"
                         FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center"
                         Foreground="{DynamicResource Sonulab.AccentBrush}"/>
            </StackPanel>
            <TextBlock Text="Reading amp metadata…" FontSize="11"
                       Foreground="{DynamicResource Sonulab.TextMutedBrush}"
                       IsVisible="{Binding IsDetailsLoading}"/>
            <TextBlock Text="No metadata — uploaded outside StompStation Manager." FontSize="11"
                       Foreground="{DynamicResource Sonulab.TextMutedBrush}"
                       IsVisible="{Binding ShowNoMetadata}"/>
            <TextBlock Text="{Binding DetailsError}" FontSize="11" TextWrapping="Wrap"
                       Foreground="{DynamicResource Sonulab.DangerBrush}"
                       IsVisible="{Binding DetailsError, Converter={x:Static ObjectConverters.IsNotNull}}"/>
            <Border Classes="rule"/>
            <ItemsControl ItemsSource="{Binding DetailsFields}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:MetadataField">
                  <Grid ColumnDefinitions="130,*" Margin="0,1">
                    <TextBlock Grid.Column="0" Classes="section-label" Text="{Binding Label}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Value}" FontSize="12" TextWrapping="Wrap"/>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel IsVisible="{Binding DetailsNotes, Converter={x:Static ObjectConverters.IsNotNull}}">
              <Border Classes="rule"/>
              <TextBlock Classes="section-label" Text="NOTES"/>
              <TextBlock Text="{Binding DetailsNotes}" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>
            <Button Content="{Binding DetailsUrl}" Command="{Binding OpenDetailsUrlCommand}"
                    Padding="0" Background="Transparent" FontSize="12" Cursor="Hand"
                    Foreground="{DynamicResource Sonulab.AccentBrush}"
                    IsVisible="{Binding DetailsUrl, Converter={x:Static ObjectConverters.IsNotNull}}"/>
            <Border Classes="rule"/>
            <StackPanel Orientation="Horizontal" Spacing="8" IsVisible="{Binding !IsEditingMetadata}">
              <Button Classes="accent-outline" Content="Edit notes/link" Command="{Binding BeginEditMetadataCommand}"
                      IsEnabled="{Binding CanMutate}" FontSize="11"/>
            </StackPanel>
            <StackPanel Spacing="4" IsVisible="{Binding IsEditingMetadata}">
              <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Link" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="11"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding EditUrl}" Margin="0,0,0,4"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Notes" VerticalAlignment="Top" Margin="0,4,8,0" FontSize="11"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EditNotes}" AcceptsReturn="True"
                         TextWrapping="Wrap" MaxHeight="96"/>
              </Grid>
              <TextBlock Text="{Binding EditBudgetWarning}" FontSize="11" TextWrapping="Wrap"
                         Foreground="{DynamicResource Sonulab.WarningBrush}"
                         IsVisible="{Binding EditBudgetWarning, Converter={x:Static ObjectConverters.IsNotNull}}"/>
              <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Classes="accent-outline" Content="Save to pedal" Command="{Binding SaveMetadataCommand}" IsEnabled="{Binding CanMutate}"
                        ToolTip.Tip="Re-flashes the slot (~3 s): backup, write, verify — the amp sound is untouched"/>
                <Button Content="Cancel" Command="{Binding CancelEditMetadataCommand}"/>
              </StackPanel>
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- ===== State 3: placeholder ===== -->
        <TextBlock Text="Select an amp to see its details."
                   Foreground="{DynamicResource Sonulab.TextMutedBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,48,0,0">
          <TextBlock.IsVisible>
            <MultiBinding Converter="{x:Static BoolConverters.And}">
              <Binding Path="!IsDetailsVisible"/>
              <Binding Path="!IsUploadPanelOpen"/>
            </MultiBinding>
          </TextBlock.IsVisible>
        </TextBlock>

      </Panel>
    </ScrollViewer>
  </DockPanel>
</UserControl>
```

- [ ] **Step 3: Rewrite `src/Sonulab.App/Views/AmpListView.axaml`** with the master-detail grid. The command bar keeps `x:Name="UploadNamButton"`/`x:Name="UploadVxampButton"` and the rename TextBox keeps `LostFocus="OnEditBoxLostFocus"` — the code-behind depends on all three. Full new content:

```xml
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Sonulab.App.ViewModels"
             xmlns:conv="using:Sonulab.App.Converters"
             xmlns:behaviors="using:Sonulab.App.Behaviors"
             xmlns:views="using:Sonulab.App.Views"
             x:Class="Sonulab.App.Views.AmpListView"
             x:DataType="vm:AmpListViewModel">
  <Grid ColumnDefinitions="260,*">

    <!-- ===== Left: command bar + slot list ===== -->
    <DockPanel Grid.Column="0">
      <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="4" Margin="0,0,0,6">
        <Button Command="{Binding RefreshCommand}" IsEnabled="{Binding CanRefresh}" ToolTip.Tip="Refresh">
          <PathIcon Data="{StaticResource Icon.Refresh}" Width="16" Height="16"/>
        </Button>
        <Button x:Name="UploadNamButton" IsEnabled="{Binding CanMutate}" ToolTip.Tip="Distill a .nam and upload it to an empty slot">
          <TextBlock Text="Upload .nam…"/>
        </Button>
        <Button x:Name="UploadVxampButton" IsEnabled="{Binding CanMutate}" ToolTip.Tip="Upload an already-distilled .vxamp to an empty slot">
          <TextBlock Text="Upload .vxamp…"/>
        </Button>
        <Button Command="{Binding DeleteCommand}" IsEnabled="{Binding CanMutate}" ToolTip.Tip="Delete (backs up first)">
          <PathIcon Data="{StaticResource Icon.Delete}" Width="16" Height="16"/>
        </Button>
      </StackPanel>

      <StackPanel DockPanel.Dock="Bottom" Margin="0,4,0,0" IsVisible="{Binding IsBusy}" Spacing="4">
        <ProgressBar IsIndeterminate="True" Height="4"/>
        <TextBlock Text="{Binding BusyMessage}" FontSize="11" Foreground="{DynamicResource Sonulab.TextMutedBrush}"/>
      </StackPanel>

      <ListBox ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}"
               IsEnabled="{Binding !IsBusy}">
        <ListBox.KeyBindings>
          <KeyBinding Gesture="F2" Command="{Binding Selected.BeginRenameCommand}"/>
        </ListBox.KeyBindings>
        <ListBox.ItemTemplate>
          <DataTemplate x:DataType="vm:AmpItemViewModel">
            <DockPanel>
              <DockPanel.ContextMenu>
                <ContextMenu>
                  <MenuItem Header="Rename" Command="{Binding BeginRenameCommand}"/>
                </ContextMenu>
              </DockPanel.ContextMenu>
              <TextBlock Classes="slot-number" Text="{Binding DisplaySlot, StringFormat='{}{0:00}'}"
                         Width="28" VerticalAlignment="Center"/>
              <Panel VerticalAlignment="Center" Margin="8,0,0,0">
                <TextBlock VerticalAlignment="Center" IsVisible="{Binding !IsEditing}"
                           Opacity="{Binding IsEmpty, Converter={x:Static conv:BoolToOpacity.Instance}}"
                           FontStyle="{Binding IsEmpty, Converter={x:Static conv:BoolToItalic.Instance}}"
                           Text="{Binding Name}"/>
                <TextBox IsVisible="{Binding IsEditing}" Text="{Binding EditName}" MaxLength="31"
                         Padding="2,0" VerticalAlignment="Center"
                         behaviors:EditBoxBehavior.FocusOnVisible="{Binding IsEditing}"
                         LostFocus="OnEditBoxLostFocus">
                  <TextBox.KeyBindings>
                    <KeyBinding Gesture="Enter"
                                Command="{Binding $parent[ListBox].((vm:AmpListViewModel)DataContext).CommitRenameCommand}"
                                CommandParameter="{Binding}"/>
                    <KeyBinding Gesture="Escape" Command="{Binding CancelRenameCommand}"/>
                  </TextBox.KeyBindings>
                </TextBox>
              </Panel>
            </DockPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </DockPanel>

    <!-- ===== Right: detail panel (upload form / details / placeholder) ===== -->
    <views:AmpDetailPanel Grid.Column="1" Margin="12,0,0,0"/>

  </Grid>
</UserControl>
```

Deletions this rewrite performs (verify nothing else vanished): the old `MaxWidth=560` DockPanel, the two top-docked message TextBlocks (now in `AmpDetailPanel`), the bottom-docked upload Border, and the bottom-docked details Border. Every binding they contained now lives in `AmpDetailPanel.axaml` — diff your new files against the pre-change `AmpListView.axaml` (`git show HEAD:src/Sonulab.App/Views/AmpListView.axaml`) and confirm every `{Binding …}` path that existed before still occurs exactly once across the two new files (except `UploadProgressValue`'s `IsIndeterminate` sibling etc., which move verbatim).

- [ ] **Step 4: Build + full suite**

Run: `dotnet build` — succeeds, no new AVLN errors (a missing `PART_ContentPresenter` or bad `MultiBinding` fails here).
Run: `dotnet test` — 312/312, zero test edits.

- [ ] **Step 5: Smoke-run the states**

`dotnet run --project src/Sonulab.App` (device optional): disconnected, the Amps tab shows list left + "Select an amp…" placeholder right. With the pedal connected: select an amp → details card fills the right panel; "Upload .nam…" → the right panel swaps to the upload form; Close → back to details/placeholder. Close the app.

- [ ] **Step 6: Commit**

```bash
git add src/Sonulab.App/Views/AmpDetailPanel.axaml src/Sonulab.App/Views/AmpDetailPanel.axaml.cs src/Sonulab.App/Views/AmpListView.axaml
git commit -m "feat: Amps tab master-detail - right panel hosts details and upload form"
```

---

### Task 3: Skin rollout (MainWindow, Presets, IRs, editor, converters)

**Files:**
- Modify: `src/Sonulab.App/Views/MainWindow.axaml`
- Modify: `src/Sonulab.App/Views/PresetListView.axaml`
- Modify: `src/Sonulab.App/Views/IrListView.axaml`
- Modify: `src/Sonulab.App/Views/ParameterEditorView.axaml`
- Modify: `src/Sonulab.App/Converters/Converters.cs`

**Interfaces:**
- Consumes: Task 1 tokens/classes only. No layout changes in this task — attribute swaps only.

- [ ] **Step 1: MainWindow top bar** — in `src/Sonulab.App/Views/MainWindow.axaml`, replace the top-bar `Border` opening tag:

```xml
    <!-- old -->
    <Border DockPanel.Dock="Top" Padding="12,8">
    <!-- new -->
    <Border DockPanel.Dock="Top" Padding="12,8"
            Background="{DynamicResource Sonulab.SurfaceAltBrush}"
            BorderBrush="{DynamicResource Sonulab.BorderBrush}" BorderThickness="0,0,0,1">
```

and give the Connect button the accent treatment:

```xml
    <!-- old -->
        <Button Command="{Binding Connection.ConnectCommand}"
                ToolTip.Tip="Connect to StompStation">
    <!-- new -->
        <Button Classes="accent-outline" Command="{Binding Connection.ConnectCommand}"
                ToolTip.Tip="Connect to StompStation">
```

(The status dot's brushes come from the converter change in Step 5; no XAML change needed for it. Nav rows pick up the global `ListBoxItem` style from Task 1 automatically.)

- [ ] **Step 2: PresetListView** — in `src/Sonulab.App/Views/PresetListView.axaml`, two attribute swaps:

Slot number (line ~71):
```xml
    <!-- old -->
            <TextBlock Text="{Binding DisplaySlot, StringFormat='{}{0:00}'}"
                       Width="28" VerticalAlignment="Center"
                       FontFamily="Consolas,Cascadia Mono,monospace"
                       Opacity="0.6"/>
    <!-- new -->
            <TextBlock Classes="slot-number" Text="{Binding DisplaySlot, StringFormat='{}{0:00}'}"
                       Width="28" VerticalAlignment="Center"/>
```

Busy text (line ~41): change `Opacity="0.7"` to `Foreground="{DynamicResource Sonulab.TextMutedBrush}"` (drop the Opacity attribute).

- [ ] **Step 3: IrListView** — in `src/Sonulab.App/Views/IrListView.axaml`:

1. Slot number: same swap as PresetListView Step 2 (the identical 4-line TextBlock at line ~89).
2. Busy text (line ~72): `Opacity="0.7"` → `Foreground="{DynamicResource Sonulab.TextMutedBrush}"`.
3. Upload panel Border (line ~35): replace
```xml
    <Border DockPanel.Dock="Bottom" Margin="8,4" Padding="10" CornerRadius="4"
            BorderThickness="1" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            IsVisible="{Binding IsUploadPanelOpen}">
```
with
```xml
    <Border Classes="card" DockPanel.Dock="Bottom" Margin="8,4"
            IsVisible="{Binding IsUploadPanelOpen}">
```
4. Hex literals: `Foreground="#D9820F"` (blocked message, line ~27) → `Foreground="{DynamicResource Sonulab.WarningBrush}"`; both `Foreground="#D9534F"` (error texts, lines ~30 and ~60) → `Foreground="{DynamicResource Sonulab.DangerBrush}"`; upload status `Opacity="0.8"` (line ~59) → `Foreground="{DynamicResource Sonulab.TextMutedBrush}"` (drop Opacity).

- [ ] **Step 4: ParameterEditorView** — in `src/Sonulab.App/Views/ParameterEditorView.axaml`:

1. Dirty dot (line ~9): `Foreground="#FF9900"` → `Foreground="{DynamicResource Sonulab.WarningBrush}"`.
2. SubGroup header (line ~54): `<TextBlock Text="{Binding Header}" FontWeight="SemiBold" Margin="0,2"/>` → `<TextBlock Classes="section-label" Text="{Binding Header}" FontWeight="SemiBold" Margin="0,4,0,2"/>`.
3. Loading overlay text (line ~92): `Opacity="0.8"` → `Foreground="{DynamicResource Sonulab.TextMutedBrush}"` (drop Opacity).

- [ ] **Step 5: Converters → theme tokens** — in `src/Sonulab.App/Converters/Converters.cs`, replace the `BoolToBrush` and `EnabledToBrush` classes with resource-resolving versions (add `using Avalonia;` and `using Avalonia.Controls;` to the file's usings if absent):

```csharp
/// <summary>
/// Converts IsConnected (bool) to a brush for the status dot Ellipse.
/// true  => Sonulab.SuccessBrush (connected)   false => Sonulab.TextMutedBrush
/// Resolves theme tokens at convert time; falls back to fixed brushes if the
/// theme layer isn't loaded (e.g. bare control tests). Note: a live OS theme
/// switch updates the dot on the next IsConnected change, not instantly.
/// </summary>
public sealed class BoolToBrush : IValueConverter
{
    public static readonly BoolToBrush Connected = new();
    public object? Convert(object? value, Type t, object? p, CultureInfo c)
        => value is true
            ? ResolveBrush("Sonulab.SuccessBrush", Brushes.LimeGreen)
            : ResolveBrush("Sonulab.TextMutedBrush", Brushes.Gray);
    public object? ConvertBack(object? v, Type t, object? p, CultureInfo c) => throw new NotSupportedException();

    internal static object ResolveBrush(string key, IBrush fallback) =>
        Application.Current is { } app &&
        app.TryGetResource(key, app.ActualThemeVariant, out var res) && res is IBrush b
            ? b : fallback;
}
```

```csharp
/// <summary>enable-state (bool?) -> brush: true => Sonulab.SuccessBrush (on), false/null => muted (off).</summary>
public sealed class EnabledToBrush : IValueConverter
{
    public static readonly EnabledToBrush Instance = new();
    public object? Convert(object? value, Type _, object? __, CultureInfo ___) =>
        value is true
            ? BoolToBrush.ResolveBrush("Sonulab.SuccessBrush", Brushes.LimeGreen)
            : BoolToBrush.ResolveBrush("Sonulab.TextMutedBrush", Brushes.Gray);
    public object? ConvertBack(object? v, Type _, object? __, CultureInfo ___) => throw new NotSupportedException();
}
```

- [ ] **Step 6: Sweep for leftovers**

Run: `grep -rn "#D9534F\|#D9820F\|#FF9900\|#3B82D6" src/Sonulab.App --include=*.axaml`
Expected: no matches (all migrated to tokens in Tasks 2–3). If any remain, migrate them the same way (`#D9534F`→Danger, `#D9820F`→Warning, `#3B82D6`→Accent).

- [ ] **Step 7: Build + full suite + smoke-run**

`dotnet build` clean; `dotnet test` 312/312; `dotnet run --project src/Sonulab.App` — all three tabs render with the warm palette, no stray gray-blue panels, Presets tab unchanged structurally. Close the app.

- [ ] **Step 8: Commit**

```bash
git add src/Sonulab.App/Views/MainWindow.axaml src/Sonulab.App/Views/PresetListView.axaml src/Sonulab.App/Views/IrListView.axaml src/Sonulab.App/Views/ParameterEditorView.axaml src/Sonulab.App/Converters/Converters.cs
git commit -m "feat: apply Studio-warm skin to top bar, Presets, IRs, editor"
```

---

### Task 4: Visual verification checklist + docs

**Files:**
- Create: `docs/HARDWARE-VALIDATION-ui-polish.md`
- Modify: `CLAUDE.md`

**Interfaces:** none (documentation).

- [ ] **Step 1: Create `docs/HARDWARE-VALIDATION-ui-polish.md`**

```markdown
# Visual validation — Studio-warm UI polish + Amps master-detail

**Spec:** `docs/superpowers/specs/2026-07-06-ui-polish-design.md`. Target mockup:
`.superpowers/brainstorm/14837-1783373193/content/final-look.html` (gitignored).

Most steps are device-free. Steps marked [PEDAL] need the StompStation connected
(VoidX-Control closed).

## Checklist

- [ ] 1. `dotnet run --project src/Sonulab.App` with Windows in DARK mode:
       window/nav/top bar are warm near-black (not gray-blue); accent everywhere is
       amber (nav selection bar+tint, focus rings, progress bars) — no stock blue.
- [ ] 2. Switch Windows to LIGHT mode (Settings > Personalization > Colors) with the
       app running: everything re-renders warm-paper; no white-on-white or unreadable
       text anywhere. Both variants intentional = spec's hard requirement.
- [ ] 3. Amps tab, disconnected: list left, "Select an amp to see its details."
       placeholder right. No bottom-docked panels remain.
- [ ] 4. [PEDAL] Connect. Amps tab: select an amp with metadata → details card right
       (name header + amber SLOT badge, ruled SOURCE/DISTILLED/NAM/NOTES sections,
       accent link, Edit button). Select a VoidX-era amp → "No metadata" state.
- [ ] 5. [PEDAL] Edit notes/link → edit sub-panel in the card; Save → busy → card
       refreshes. Cancel works. Budget warning appears for a huge pasted note.
- [ ] 6. [PEDAL] Upload .nam… → right panel swaps to the upload form (details hidden);
       progress; Done → panel returns to the new amp's details. Close mid-form returns
       to details/placeholder. Upload while another amp's details showing: no overlap.
- [ ] 7. Presets tab: list + parameter editor unchanged structurally; slot numbers
       muted monospace; selection amber; dirty dot warm amber; expander sub-headers
       are small-caps-style labels.
- [ ] 8. IRs tab: single column (unchanged layout); upload panel is a warm card;
       messages use warning/danger tokens.
- [ ] 9. F2 rename still works in all three lists (in-place edit box, Enter commits,
       Esc cancels) — the ListBoxItem restyle must not break the edit TextBox.
- [ ] 10. Screenshot the Amps tab in both variants; compare against the mockup;
       attach or note deviations here. Record date + pass/fail per step.
```

- [ ] **Step 2: Update `CLAUDE.md`**

1. In the Architecture bullet for `src/Sonulab.App`, after `embedded labels.en.json + hidden-params.json + Icons.axaml` append: `+ Styles/SonulabTheme.axaml (Studio-warm palette tokens & style classes — use tokens, never hex literals in views)`.
2. In "Critical conventions & gotchas", append a line: `- UI colors come from Styles/SonulabTheme.axaml tokens (Sonulab.*Brush, both theme variants) — never hardcode hex in .axaml; Fluent accent ramp is overridden in App.axaml.`
3. In "Not done", append: `UI-polish visual checklist (docs/HARDWARE-VALIDATION-ui-polish.md) pending.`

- [ ] **Step 3: Full suite one last time**

Run: `dotnet test` — 312/312.

- [ ] **Step 4: Commit**

```bash
git add docs/HARDWARE-VALIDATION-ui-polish.md CLAUDE.md
git commit -m "docs: UI-polish visual checklist + theme conventions in CLAUDE.md"
```

---

## Self-review notes

- Spec coverage: §1.1 tokens + §1.2 accent overrides + §1.3 classes + Window/SplitView chrome (Task 1); §2 master-detail with three states, message placement, binding-preservation check (Task 2); §3 rollout incl. §1.4 hex migration and converter change (Task 3); §5 verification checklist + CLAUDE.md (Task 4). §4 invariants are enforced by the Global Constraints (zero VM/test changes, 312/312 after every task).
- The one uncertain platform behavior (Fluent accent override timing) has an explicit in-task verification step with a concrete fallback, not a TBD.
- Type/name consistency: token names in Tasks 2–3 match Task 1's produced list verbatim; `AmpDetailPanel` x:Class matches its code-behind; preserved x:Names (`UploadNamButton`, `UploadVxampButton`) and `OnEditBoxLostFocus` match the untouched code-behind.
