// Consumes the Windows-only ToneManager.Tone3000 library (DPAPI token store); this dev tool
// only ever runs on the Windows dev box, so declare the same platform.
[assembly: System.Runtime.Versioning.SupportedOSPlatform("windows")]
